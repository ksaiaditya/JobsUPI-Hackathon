### Server environment example
# Copy to .env and adjust values. Never commit real secrets.

# Express / Socket.IO port
PORT=5000

# MongoDB connection string
MONGO_URI=mongodb://127.0.0.1:27017/jobs_upi

# JWT signing secret (set a long random string in real env)
JWT_SECRET=replace_me_with_a_long_random_string_here

# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173

# Socket.IO path (optional customization)
SOCKET_PATH=/socket.io

# QR session auto-expiration minutes (optional; fallback handled client-side)
QR_SESSION_TTL_MIN=60
